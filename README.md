
##  Features

- Inheritance using a base `Vehicle` class
- Method overriding (`move()` method in child classes)
- Method overloading (`calculateFuel()` in `Car`)
- Polymorphism using parent references
- Custom `toString()` implementation
- String parsing using `StringTokenizer`
- Dynamic string building using `StringBuffer`

---

